.breadcrumb-container {
  padding: 25px;

  & .breadcrumb a {
    color: #007bff;

    &:hover {
      text-decoration: underline;
    }
  }

  & .breadcrumb {
    color: #6c757d;
  }

  & .divider {
    padding: 0 10px;
  }
}

@media (max-width: env(--device-width-tablet-min)) {
  .breadcrumb-container {
    padding-left: 10px;
  }
}
